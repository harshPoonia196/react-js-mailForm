// import MailSender from "./mailForm";

// export { MailSender };

// rm -rf dist && mkdir dist &&  babel src/reactJsMailer -d dist --copy-files

import MailForm from "./mailForm";

export { MailForm };

// import { baseUrl, client } from "../config";

// const apiCall = async (data) => {
//   const url = `${baseUrl}/react-js-mailer`;

//   const res = await client.post(url, data);

//   console.log(" res ===========>", res);
// };

// export default function MailSender(props) {
//   const { ...data } = props || {};

//   apiCall(data);
// }
